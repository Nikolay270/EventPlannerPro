@model IEnumerable<EventPlannerPro.Models.City>
@{
    ViewData["Title"] = "Градове";
    bool isAdmin = User.IsInRole("Admin");
}

<div class="container-fixed">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>@ViewData["Title"]</h2>
        <a asp-action="Create" class="btn-custom">Създаване на нов град</a>
    </div>

    @if (!Model.Any())
    {
        <p class="text-muted">Няма намерени градове.</p>
    }
    else
    {
        <!-- Force exactly 3 columns -->
        <div class="row row-cols-3 g-4">
            @foreach (var city in Model)
            {
                <div class="col">
                    <div class="activity-card text-center">
                        @if (!string.IsNullOrEmpty(city.PhotoUrl))
                        {
                            <img src="@city.PhotoUrl" alt="@city.Name" class="card-img-top" />
                        }
                        <div class="activity-card-body">
                            <h5>@city.Name</h5>
                        </div>
                        <div class="activity-card-footer">
                            <a asp-controller="Activities"
                               asp-action="ByCity"
                               asp-route-cityId="@city.Id"
                               class="btn-custom btn-sm">
                                Виж събития
                            </a>
                            @if (isAdmin)
                            {
                                <a asp-action="Edit" asp-route-id="@city.Id" class="btn-custom btn-sm">Редактирай</a>
                                <form asp-action="Delete"
                                      asp-route-id="@city.Id"
                                      method="post"
                                      class="d-inline">
                                    <button type="submit"
                                            class="btn-custom btn-sm"
                                            onclick="return confirm('Наистина ли желаете да изтриете този град?');">
                                        Изтрий
                                    </button>
                                </form>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>
