@model IEnumerable<EventPlannerPro.Models.City>
@{
    ViewData["Title"] = "Browse Cities";
    bool isAdmin = User.IsInRole("Admin");
}

<div class="container-fixed">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Browse Cities</h2>
        <a asp-action="Create" class="btn-custom">Create New City</a>
    </div>

    @if (!Model.Any())
    {
        <p class="text-muted">No cities found.</p>
    }
    else
    {
        <!-- Force exactly 3 columns -->
        <div class="row row-cols-3 g-4">
            @foreach (var city in Model)
            {
                <div class="col">
                    <div class="activity-card text-center">
                        @if (!string.IsNullOrEmpty(city.PhotoUrl))
                        {
                            <img src="@city.PhotoUrl" alt="@city.Name" class="card-img-top" />
                        }
                        <div class="activity-card-body">
                            <h5>@city.Name</h5>
                        </div>
                        <div class="activity-card-footer">
                            <a asp-controller="Activities"
                               asp-action="ByCity"
                               asp-route-cityId="@city.Id"
                               class="btn-custom btn-sm">
                                View Activities
                            </a>
                            @if (isAdmin)
                            {
                                <a asp-action="Edit" asp-route-id="@city.Id" class="btn-custom btn-sm">Edit</a>
                                <form asp-action="Delete"
                                      asp-route-id="@city.Id"
                                      method="post"
                                      class="d-inline">
                                    <button type="submit"
                                            class="btn-custom btn-sm"
                                            onclick="return confirm('Delete this city?');">
                                        Delete
                                    </button>
                                </form>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>
